#ifndef DEPOSIT_H
#define DEPOSIT_H

#include <iostream>
#include <cmath>
#include <stdio.h>
#include <cmath>

using namespace std;

int verifyDeposit(int x);
int verifyDays(int d);
int calculateDohod(int d, int x, int s);

#endif